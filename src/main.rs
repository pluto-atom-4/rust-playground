use std::cmp::Ordering;
use std::io;
use rand::prelude::thread_rng;
use rand::Rng;


fn main() {
    let number = 3;
    if number != 0 { // explicitly check for inequality to zero
        println!("condition was something other than zero");
    }
}
